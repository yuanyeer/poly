# Paper mode is read-only. Do not put live trading secrets here.
# Copy to `.env` locally if you want to override paths; never commit `.env`.

POLY_CONFIG=config/paper.yaml
POLY_LEDGER_PATH=data/paper_ledger.jsonl

# Optional public endpoint overrides (no auth).
# POLY_CLOB_HOST=https://clob.polymarket.com
# POLY_GAMMA_HOST=https://gamma-api.polymarket.com
# POLY_CHAIN_ID=137

# Intentionally omitted — paper mode must never load these:
# POLYMARKET_PRIVATE_KEY=
# CLOB_API_KEY=
# CLOB_SECRET=
# CLOB_PASS_PHRASE=
